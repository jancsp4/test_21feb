name: Push Workflow

on:
  push:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run build
        run: echo "Building on push event"

      - name: Run tests
        run: echo "Running tests"
